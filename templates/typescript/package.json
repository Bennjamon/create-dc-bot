{
  "name": "bot",
  "main": "dist/index.js",
  "version": "1.0.0",
  "description": "A bot created with create-discord-bot",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dist": "npm run build && npm start",
    "dev": "ts-node-dev --files --ignore-watch src/commands"
  },
  "license": "MIT",
  "dependencies": {
    "discord.js": "13.1.0",
    "dotenv": "10.0.0"
  },
  "devDependencies": {
    "@types/node": "16.9.1",
    "ts-node-dev": "1.1.8",
    "typescript": "4.4.3"
  }
}
